[www]
pm = dynamic
pm.max_children = 50
pm.start_servers = 10
pm.min_spare_servers = 5
pm.max_spare_servers = 20
pm.max_requests = 500

; max_input_time เป็นค่า PHP (ไม่ใช่ directive ของ pm.*)
php_admin_value[max_input_time] = 300

request_terminate_timeout = 300s
